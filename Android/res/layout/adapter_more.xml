<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/adapter_coupon"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center_vertical"
    android:background="@color/white"
    android:padding="4dp" >

    <ImageView
        android:id="@+id/category_image"
        android:layout_width="@dimen/more_adapter_images_widht"
        android:layout_height="@dimen/more_adapter_images_height"
        android:layout_alignParentLeft="true"
        android:adjustViewBounds="false"
        android:paddingRight="5dp" />

    <TextView
        android:id="@+id/more_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/category_image"
        android:gravity="center_vertical"
        android:paddingTop="5dp"
        android:textColor="@color/gray_dark"
        android:textSize="@dimen/more_adapter_text" />

</RelativeLayout>